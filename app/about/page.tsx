import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'About | Reading Log',
  description: 'Keep track of your reads',
};

export default function AboutPage() {
  return (
    <div>
      <h1> About Reading Log</h1>
    </div>
  );
}
